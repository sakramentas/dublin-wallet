import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  hero: {
    flex: 1,
    width: '100%',
    // height: 100,
    flexDirection: 'row',
    backgroundColor: '#0aa812',
    alignItems: 'center',
    justifyContent: 'center',
  },
  heroText: {
    fontSize: 24,
    color: '#FFF',
  },
});
