import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  schoolList: {
    flex: 3,
    width: '100%',
    height: 100,
    paddingTop: 20,
    flexDirection: 'column',
    alignItems: 'center',
  },
});
